"use client";

import React from 'react';
import {NextPage} from "next";
import PasswordResetTokenPage from "@/app/(landing)/(auth)/password-reset/[token]/page";

const PasswordResetPage: NextPage = () => {

    return (
        <><PasswordResetTokenPage /></>
    );
};

export default PasswordResetPage;
