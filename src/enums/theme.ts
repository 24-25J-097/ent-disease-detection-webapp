/**
 * ```jsx
 *  When changing the enum, also update the MenuType in Theme.d.ts
 * ```
 */
export enum MenuTypes {
    SIDE = 'side',
    SIMPLE = 'simple',
    TOP = 'top',
}

/**
 * ```jsx
 *  When changing the enum, also update the ThemeMode in Theme.d.ts
 * ```
 */
export enum ThemeModes {
    LIGHT = 'light',
    DARK = 'dark',
    SYSTEM = 'system',
}

/**
 * ```jsx
 *  When changing the enum, also update the ColorTheme in Theme.d.ts
 * ```
 */
export enum ColorThemes {
    DEFAULT = 'default',
}
