export enum Role {
    ADMIN = "admin",
    DOCTOR = "doctor",
    RADIOLOGIST = "radiologist",
    STUDENT = "student",
}

export enum Permission {

}

export enum AccessKey {
    ENTI_TOKEN = "enti-ax-token",
    ENTI_ROLE = "enti-ax-role",
}
